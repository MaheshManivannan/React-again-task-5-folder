const Header = () => {
    const styles = {
        backgroundColor: "red",
        color: "white",
        fontWeight: "bold"
    }

    return (
        <h2 style={styles}>TO DO LIST</h2>
    );
}
export default Header;